# acquis.yml — says to CrowdSec which logs to analyze
# check the logs Traefik through Docker

source: docker
container_name:
  - fp-collector-traefik-1   # name of the Traefik container
labels:
  type: traefik